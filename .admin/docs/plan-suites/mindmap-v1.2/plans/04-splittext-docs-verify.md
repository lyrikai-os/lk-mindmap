# 04 — SplitText + Docs verify + package

## Goal and stop condition

Docs heading/body split reveals; user-guide delta names **v1.2**; verify matrix recorded; package smoke note. END-GOAL demonstrable.

## Prerequisites

- Units 01–03 complete.

## Walls

In scope: Docs `h2` / eyebrow / short intro SplitText; `guides/user-guide-delta.md`; verification slots; package.json `1.2.0`; `npm run typecheck|test|test:e2e|build` (+ package if practical).

Out of scope: SplitText on canvas text; ScrambleText; tip writes.

## Ordered steps

1. On Docs open, SplitText chars/words on title (+ short intro); stagger ≤ shell budget; `revert()` on cleanup.
2. Reduced-motion: no split/stagger — final text state immediately.
3. Update Docs / user-guide delta for v1.2 motion behavior.
4. Fill `guides/verification.md` pending→recorded.
5. Ensure package version **1.2.0**; run verify matrix; package if practical.

## Verification

Full matrix in verification guide. END-GOAL checklist.

## Evidence / handoff

Record exits, limitations, preview URL/port if used.

## Next dependency

None — unit close; Orchestrator Adversary → Audit → Closeout Phase A→B gate.
